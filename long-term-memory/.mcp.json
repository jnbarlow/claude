{
  "mcpServers": {
    "ltm-postgres": {
      "command": "/bin/bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/start-mcp.sh"],
      "env": {
        "CLAUDE_PLUGIN_OPTION_LTM_DB": "${user_config.LTM_DB}"
      }
    }
  }
}
